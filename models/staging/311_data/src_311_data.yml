version: 2

sources:
  - name: 311_data
    tables:
      - name: stg_agency
        columns:
          - name: agency_dim_id
            tests: 
              - unique
              - not_null
      - name: stg_complaint_type
        columns:
          - name: complaint_type_dim_id
            tests: 
              - unique
              - not_null
      - name: stg_channel_type
        columns:
          - name: channel_type_dim_id
            tests: 
              - unique
              - not_null